# Apache Airflow REST API connection
# Use /api/v2 for Airflow 3.x (JWT auth) or /api/v1 for Airflow 2.x (basic auth).
AIRFLOW_BASE_URL=http://100.x.x.x:8080/api/v2
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=
